<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobiHive.MainPage"
             Title="Ana Sayfa">
    <Grid>
        <Grid RowDefinitions="Auto,*" BackgroundColor="#6200EE">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- زر فتح القائمة الجانبية -->
            

            
        </Grid>

        <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
            <Image Source="your_image.jpg" WidthRequest="200" HeightRequest="200"/>
            <Label Text="YAHYA S.A. ABUZAHRA" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center"/>
            <Label Text="Bartin Üniversitesi Bilgisayar Mühendisliği" FontSize="14" HorizontalOptions="Center"/>
            <Label Text="ÖDEV-1" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center"/>
        </StackLayout>
    </Grid>
</ContentPage>
